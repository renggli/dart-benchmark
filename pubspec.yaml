name: benchmark
version: 0.1.0

homepage: https://github.com/renggli/dart-benchmark
description: A time and space efficient collection of data structures and
  algorithms to deal with data in Dart.

environment:
  sdk: ^3.2.0
dependencies:
  collection: ^1.18.0
  data: ^0.12.0
  meta: ^1.11.0
  more: ^4.1.0
  petitparser: ^6.0.0
dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
dependency_overrides:
  more:
    git: https://github.com/renggli/dart-more